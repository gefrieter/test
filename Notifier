pragma solidity ^0.6.6;

contract Notifier {
    event ContractDeployed(address indexed deployedContract);

    function notify(address deployedContract) public {
        emit ContractDeployed(deployedContract);
    }
}
